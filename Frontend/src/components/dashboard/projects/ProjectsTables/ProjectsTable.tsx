import { TableProps } from 'antd';
import { Projects } from '../../../../types';

type Props = {
  data: Projects[];
} & TableProps<any>;

export const ProjectsTable = ({}: Props) => {
  return <></>;
};
